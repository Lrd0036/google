import assert from 'node:assert/strict'; import test from 'node:test'; import { buildMeta,digestJson } from './meta.mjs';
const now=new Date('2026-08-27T12:00:00Z'); const report={schema:'runbookbench-report/v0.1',safety:'PASS',publishable_corpus:true,generated_at:'2026-08-27T11:00:00Z',corpus_sha256:`sha256:${'c'.repeat(64)}`,submission_sha256:`sha256:${'d'.repeat(64)}`};
test('LIVE requires IAP evidence and current Control reachability',()=>{ assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'jwt',controlReachable:true,checkedAt:now,report}).data_mode,'LIVE'); assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'',controlReachable:true,checkedAt:now,report}).data_mode,'OFFLINE'); assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'jwt',controlReachable:false,checkedAt:now,report}).data_mode,'OFFLINE'); });
test('passing report cannot survive missing, stale, or mismatched evidence',()=>{ assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'jwt',controlReachable:true,checkedAt:now,report,expectedReportDigest:digestJson(report)}).benchmark.status,'PASS'); assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'jwt',controlReachable:true,checkedAt:now}).benchmark.status,'NO_CURRENT_EVIDENCE'); assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'jwt',controlReachable:true,checkedAt:new Date('2026-08-29T12:00:00Z'),report}).benchmark.status,'NO_CURRENT_EVIDENCE'); assert.equal(buildMeta({configuredMode:'LIVE',iapJwt:'jwt',controlReachable:true,checkedAt:now,report,expectedReportDigest:'sha256:wrong'}).benchmark.status,'NO_CURRENT_EVIDENCE'); });
