# RUNBOOK COMPILER — CLAIMS & EVIDENCE

This is the product-proof dashboard. A checked claim requires a reproducible
artifact and a command or test that produced it. Local/emulator evidence is not
cloud-production evidence.

## Core claims

- [x] **CLAIM 1 — Human procedural language can be compiled into constrained RBIR.** `[LOCAL]`

  Evidence: `.local/royal-duke-cooling-incident.rbir.json`, generated by
  `pnpm local:compile` from
  `fixtures/runbooks/royal-duke-cooling-incident.md`, the reviewed compile plan,
  and `fixtures/manifests/royal-duke-operations.json`.

- [x] **CLAIM 2 — Ambiguous operational policy fails compilation instead of being silently guessed.** `[UNIT] [LOCAL] [ADVERSARIAL]`

  Evidence: `packages/compiler/src/analyzer/linter.ts` and compiler tests for
  `RBK-104` (`AMBIGUOUS_PREDICATE`), `RBK-201` (`UNBOUNDED_RETRY`), and
  `RBK-301` (`UNKNOWN_CAPABILITY`).

- [x] **CLAIM 3 — The compiler cannot create executable authority outside the Capability Manifest.** `[UNIT] [LOCAL]`

  Evidence: manifest-binding linter checks and broker negative tests for
  undeclared capabilities, manifest mismatch, and destination restrictions.

- [x] **CLAIM 4 — A compiled runbook can autonomously recover a known failure (local proof).** `[LOCAL]`

  Evidence: the `pnpm local:smoke` output records the Royal Duke adapter side
  effects, per-mutation verification trace, approval suspension, and
  `COMPLETED` result after approved resume. This is local-only evidence; cloud execution remains
  intentionally disabled.

- [x] **CLAIM 5 — A human-approval node suspends execution and requires an authorized human before continuation (local proof).** `[LOCAL] [EMULATOR] [ADVERSARIAL]`

  Evidence: the local approval/audit smoke and Royal Duke execution trace; the
  approval path records suspension before restoration and authorized resume.
  The approval/audit suite also covers expired/context-mismatched rejection,
  authorized approval, cursor resume, replay rejection, and valid audit-chain
  observations.

- [x] **CLAIM 6 — The Broker's local execution model reconciles uncertain side effects without duplicating mutations.** `[UNIT]`

  Evidence: idempotency, reconciliation, replay, and uncertainty tests in
  `apps/broker/src/broker.test.ts`.

- [x] **CLAIM 7 — Adversarial input cannot expand executable authority beyond compiled policy in the local runtime.** `[UNIT] [LOCAL] [ADVERSARIAL] [LIVE-MODEL]`

  Evidence: adversarial RunbookBench fixtures and runtime-negative checks, plus
  Model judgment remains separately testable, but the maintained product demo
  is now the Royal Duke local execution path. This is local-runtime evidence,
  not a cloud Control deployment.

- [ ] **CLAIM 8 — RunbookBench independently evaluates compiler output against human-adjudicated institutional prose.** `[HUMAN-ADJUDICATED]`

  Evidence: evaluator and corpus validation are implemented, but the current
  corpus is not publishable: all 12 items remain `ANNOTATION_PENDING` and no
  human adjudication has been supplied.

- [x] **CLAIM 9 — The Royal Duke map is a real-time projection of Control and OT-sim state, not a manually advanced presentation.** `[LOCAL] [BROWSER]`

  Evidence: `GET /api/v1/events` streams revisioned state snapshots; the range
  smoke subscribes before each action and verifies the corresponding state
  event; attached browser mode disables every chapter button and replaces the
  transport with `CONTROL STREAM · LIVE`. Playwright confirmed that pressing
  `BEGIN ATTACK` in the Control Panel moved the map from `Normal operations` to
  `Trusted vendor session` without a scene click. Screenshot:
  `experience/royal-duke/output/playwright/royal-duke-control-stream.png`.

## Current product milestone

The Royal Duke cooling-plant runbook now completes through the actual hybrid
path, including a live adversarial agent event and the approval boundary:

```text
Markdown → RBIR → live ADK fleet → Model Armor → Control → Broker → Royal Duke OT-sim → HUMAN_APPROVAL → deterministic VERIFY → report bundle
```

Observed trace:

```text
classify_incident → read_process_state → preserve_session → verify_preserve → contain_remote_writes → verify_containment → prepare_restoration → verify_preparation → approve_restoration → restore_pump → verify_pressure → resolved
```

Verified exercise `rdx_c5e1762c-8030-4a32-9178-6b34ffd466ed` produced:

- deterministic campaign counts `214 / 147 / 39 / 17 / 7 / 4`;
- Shadow Analyst decision `SENSOR_FAULT` from the raw hostile instruction;
- authoritative fleet decision `OPERATOR_VIEW_INTEGRITY_FAILURE` from trusted evidence;
- Model Armor `MATCH_FOUND` with local persisted verdict-event
  `model-armor-e7af25c9-7449-46aa-b0e3-cd74862515d8`;
- a visibly blocked post-containment controller write;
- independent recovery verification above 58 PSI for 30 seconds;
- valid chained incident events and report digest
  `sha256:0b8ca911ece781d94d39d6d5580385a840d5148f86c1d6fb8bfac1f15927409a`;
- content-addressed bundle digest
  `sha256:5949779389ca11f8d8a7f82f8bf66ced463cc7bcf40789c196ad6afcba0777d4`;
- ten of ten live provenance checks verified, including trace
  `bf4ad846a3db481db89cd3ab197583a3`.
- the private bounded adapter is deployed as Cloud Run revision
  `royal-duke-worker-00002-zpd`; the superseded `acme-worker` service and
  service account were removed after the replacement passed its authenticated
  health check.

The managed resources were verified from live APIs on 2026-08-27/28. The
physical system remains the fictional local OT-sim range. The end-to-end proof
uses a local Control/bridge development process with real cloud state and agent
services; it is not a claim that the current Cloud Run release controls a
production plant.

## Next proof artifacts

1. Capture the generated compiler/submission bundle for one human-adjudicated golden case.
2. Add the intentional ambiguity mutation and record `RBK-104` plus `RBK-201` bounded-retry diagnostics.
3. Capture approval suspension, authorized resume, and prompt-injection rejection traces.
4. Repeat the same proof against the controlled cloud path only after release attestation eligibility is achieved.
