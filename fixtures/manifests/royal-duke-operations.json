{
  "manifest_version": "rb-capabilities/v0.1",
  "id": "royal-duke-operations",
  "version": 1,
  "capabilities": [
    { "id": "read_process_state", "version": 1, "description": "Read the bounded Royal Duke process snapshot.", "semantic_actions": ["confirm pump state", "read process state", "read independent pressure"], "mode": "READ", "risk": "R0_OBSERVE", "transport": { "type": "HTTP", "service": "royal-duke-worker", "method": "GET", "path": "/capabilities/process-state", "audience": "http://royal-duke-worker:8082", "allowed_host": "royal-duke-worker:8082" }, "input_schema": { "type": "object", "additionalProperties": false }, "output_schema": { "type": "object", "required": ["site", "pump_id", "pump_state", "independent_pressure_psi", "operator_pressure_psi"] }, "timeout_ms": 3000, "idempotency": { "strategy": "NATIVE_KEY", "same_key_replay_safe": true }, "approval_floor": "PREAPPROVED_RUNBOOK", "credential_profile": "royal-duke-observe-v1" },
    { "id": "preserve_session", "version": 1, "description": "Preserve the active privileged-session record.", "semantic_actions": ["preserve session", "preserve privileged session", "record session"], "mode": "WRITE", "risk": "R1_REVERSIBLE_LOW", "transport": { "type": "HTTP", "service": "royal-duke-worker", "method": "POST", "path": "/capabilities/preserve-session", "audience": "http://royal-duke-worker:8082", "allowed_host": "royal-duke-worker:8082" }, "input_schema": { "type": "object", "additionalProperties": false, "required": ["pump_id"], "properties": { "pump_id": { "type": "string" } } }, "output_schema": { "type": "object", "required": ["operation_id", "status"] }, "timeout_ms": 5000, "idempotency": { "strategy": "NATIVE_KEY", "same_key_replay_safe": true }, "approval_floor": "PREAPPROVED_RUNBOOK", "credential_profile": "royal-duke-evidence-v1" },
    { "id": "contain_remote_writes", "version": 1, "description": "Contain additional remote controller changes from the affected access path.", "semantic_actions": ["contain remote writes", "block remote controller changes"], "mode": "WRITE", "risk": "R2_STATEFUL", "transport": { "type": "HTTP", "service": "royal-duke-worker", "method": "POST", "path": "/capabilities/contain-remote-writes", "audience": "http://royal-duke-worker:8082", "allowed_host": "royal-duke-worker:8082" }, "input_schema": { "type": "object", "additionalProperties": false, "required": ["pump_id"], "properties": { "pump_id": { "type": "string" } } }, "output_schema": { "type": "object", "required": ["operation_id", "status"] }, "timeout_ms": 5000, "idempotency": { "strategy": "NATIVE_KEY", "same_key_replay_safe": true }, "approval_floor": "PREAPPROVED_RUNBOOK", "credential_profile": "royal-duke-containment-v1" },
    { "id": "prepare_restoration", "version": 1, "description": "Notify the duty plant operator and prepare, without executing, pump restoration.", "semantic_actions": ["prepare restoration", "notify duty plant operator"], "mode": "WRITE", "risk": "R1_REVERSIBLE_LOW", "transport": { "type": "HTTP", "service": "royal-duke-worker", "method": "POST", "path": "/capabilities/prepare-restoration", "audience": "http://royal-duke-worker:8082", "allowed_host": "royal-duke-worker:8082" }, "input_schema": { "type": "object", "additionalProperties": false, "required": ["pump_id"], "properties": { "pump_id": { "type": "string" } } }, "output_schema": { "type": "object", "required": ["operation_id", "status"] }, "timeout_ms": 5000, "idempotency": { "strategy": "NATIVE_KEY", "same_key_replay_safe": true }, "approval_floor": "PREAPPROVED_RUNBOOK", "credential_profile": "royal-duke-operator-v1" },
    { "id": "restore_pump", "version": 1, "description": "Restore P-101 to its approved operating state.", "semantic_actions": ["restore pump", "energize P-101"], "mode": "WRITE", "risk": "R3_HIGH_IMPACT", "transport": { "type": "HTTP", "service": "royal-duke-worker", "method": "POST", "path": "/capabilities/restore-pump", "audience": "http://royal-duke-worker:8082", "allowed_host": "royal-duke-worker:8082" }, "input_schema": { "type": "object", "additionalProperties": false, "required": ["pump_id"], "properties": { "pump_id": { "type": "string", "enum": ["P-101"] } } }, "output_schema": { "type": "object", "required": ["operation_id", "status", "process"] }, "timeout_ms": 5000, "idempotency": { "strategy": "NATIVE_KEY", "same_key_replay_safe": true }, "approval_floor": "OPERATIONS_LEAD", "credential_profile": "royal-duke-controller-v1" },
    { "id": "verify_pressure_stable", "version": 1, "description": "Verify independent pressure is above 58 PSI and stable for 30 seconds.", "semantic_actions": ["verify pressure", "verify stable pressure"], "mode": "READ", "risk": "R0_OBSERVE", "transport": { "type": "HTTP", "service": "royal-duke-worker", "method": "GET", "path": "/proof/process", "audience": "http://royal-duke-worker:8082", "allowed_host": "royal-duke-worker:8082" }, "input_schema": { "type": "object", "additionalProperties": false }, "output_schema": { "type": "object", "required": ["verification", "independent_pressure_psi"] }, "timeout_ms": 65000, "idempotency": { "strategy": "NATIVE_KEY", "same_key_replay_safe": true }, "approval_floor": "PREAPPROVED_RUNBOOK", "credential_profile": "royal-duke-verify-v1" }
  ]
}
